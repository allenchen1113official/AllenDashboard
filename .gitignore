# ── config.js ─────────────────────────────────────────────
# config.js is ignored — credentials are now served via Vercel serverless
# function at /api/config (see api/config.js) using Vercel Environment Variables.
#
# Local dev fallback: copy config.example.js → config.js and use npx vercel dev,
# or serve locally with:  npx serve . (app falls back to localStorage mode).
/config.js

# ── Vercel local dev ───────────────────────────────────────
.vercel

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# node_modules (if serve or other tools are used locally)
node_modules/
